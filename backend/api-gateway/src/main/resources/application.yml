server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/api/products/**, /api/brands/**, /api/categories/**, /api/reviews/**
        # Add a new route for the test endpoint
        - id: product-service-test
          uri: lb://product-service
          predicates:
            - Path=/api/test, /test
      discovery:
        locator:
          enabled: true
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"